---
theme: default
title: "第12回: 実プロダクト設計レビューとADR"
---

# 第12回: 実プロダクト設計レビューと ADR

現在の各設計判断が、どの問題と制約に対応しているかを説明する

<!--
話すこと:
- 最終回は「Vertical Slice + Hexagonal を正当化する」回ではない。
- 不要になった境界、追加すべき境界、見直し条件まで残す回にする。
-->
---

<DiscussionQuestion>
現在の各設計判断は、どの問題と制約に対応しているのか？
</DiscussionQuestion>

<!--
話すこと:
- 既存構成を擁護するのではなく、今後も見直せる判断基準を作る。
-->
---

## この回で目指す状態

今日のゴール:

- 設計判断を軸ごとに説明できる
- 採用しなかった選択肢も説明できる
- 見直し条件を ADR に残せる
- 実コードから改善候補を見つけられる

<!--
話すこと:
- 第12回では新しいパターンを増やさない。
- ここまでの語彙を使って、実プロダクトをレビューする。
-->
---

## 持ち込む材料

実コードから、次の例を1つ以上必ず持ち込む。

- 変更が多い Slice
- Prisma が直接見えている Use Case
- 外部 API を呼ぶ Use Case
- 複数 Slice から参照される Domain
- transaction 境界が曖昧な処理
- Queue を使っている処理

架空の請求書だけでは到達度を測れない。
実際の判断に戻る。

<!--
話すこと:
- 実コードを全員で読む必要はない。講師が短い断片にして持ってくる。
- 企業秘密や巨大なコードをスライドに貼らず、判断材料だけ抽出する。
-->
---

## 設計軸レビュー: コード・業務・依存

| 観点 | 確認する問い |
|---|---|
| コード配置 | 技術責務、機能、ユースケースのどれに近づけるか |
| 変更境界 | end-to-end な Slice として独立させるか |
| 依存境界 | 外部技術や失敗契約をどこで変換するか |
| 業務ロジック | Transaction Script / Domain Model のどちらが複雑さに合うか |

<!--
話すこと:
- まずコードの置き方と業務ルールの置き場を見る。
- ここでは永続化や通信の細部に進まず、変更がどこへ波及するかを確認する。
-->
---

## 設計軸レビュー: 永続化・整合性

| 観点 | 確認する問い |
|---|---|
| Write側永続化 | ORM直接か、Repository境界を置くか |
| Read側取得 | ORMを直接使うか、専用Queryを置くか |
| Read側モデル | Write Modelを使うか、専用Read Modelを持つか |
| Mapper | DB型と業務型を分けるか |
| 整合性境界 | どのユースケース操作を原子的変更として扱うか |
| transaction境界の決定者 | Application Service など、どの層が開始、終了を決めるか |
| transaction実装方法 | ORM API、Transaction Runner、宣言的 transaction のどれを使うか |
| 変更追跡 | 明示保存か、Change Tracking を利用するか |

<!--
話すこと:
- 「保存の入口」「読み取り最適化」「型変換」「原子的変更」を混ぜない。
- transactionを張るだけで不変条件が守られるとは限らないため、制約や冪等性も見る。
-->
---

## 設計軸レビュー: 通信・実行・所有

| 観点 | 確認する問い |
|---|---|
| 応答方式 | 呼び出し元が最終処理完了を待つか |
| 実行配置 | 同一プロセスか、別プロセスか |
| 連携方式 | 直接呼び出し、HTTP/RPC、Messaging のどれか |
| 配信形態 | Messaging を選ぶ場合、Point-to-point か Publish-subscribe か |
| Messaging infrastructure | Messaging を選ぶ場合、どの Broker / Queue service を使うか |
| Message semantics | Command か Event か |
| Delivery semantics | at-most-once / at-least-once をどう扱うか |
| デプロイ境界 | 同時リリースか、独立リリースか |
| データ所有境界 | 誰が書き込み責任を持つか |
| サービス境界 | 独立した契約と運用責任を持つか |
| チーム所有境界 | 誰が変更、運用するか |

Repository 内部で Mapper を使う、Use Case が transaction 境界を決めて Transaction Runner で実行する、Command Message を Queue で配送する、という組み合わせは成立する。
すべての軸について設計判断を作る必要はない。
Driver と問題に関係する軸だけを選び、判断する。

<!--
話すこと:
- ここは選択肢表ではなく、確認する問いの一覧として扱う。
- 共存するものを排他的な選択肢として扱わない。
- 最終回では「どの軸の話か」を分けられるかを見る。
- 表を全部埋めることが目的ではない。Driverに関係しない軸は「今回は対象外」としてよい。
-->
---

## ADR に残す項目

```md
# ADR: 請求書更新とメール送信要求を同一DB transactionで永続化する

## Status
Proposed / Accepted / Superseded

## Date
YYYY-MM-DD

## Owners
判断を保守する人、チーム

## Decision
何を選ぶか

## Context
どの変更、障害、制約が問題だったか

## Reason / Rationale
なぜその軸でその選択をしたか

## Consequences / Trade-offs
良い結果と悪い結果。増える実装量、学習コスト、運用コスト

## Decision Rules / Constraints
この決定を適用する範囲、例外、守る規則

## Review Conditions
どの条件で再検討するか

## Rejected Options
なぜ選ばないか

## Related Decisions
関連ADR

## Follow-up Decisions
このADRでは固定しない、別途決める判断
```

<!--
話すこと:
- ADR は立派な作文ではない。あとで判断を追えるようにするための記録。
- Context を抽象化しすぎない。具体的な変更頻度、障害、制約を書く。
-->
---

## 個人ワーク: 実コードをレビューする

参加者ごと、またはペアごとに、実コードの一箇所を選ぶ。
これは ADR ではなく、Architecture Review Worksheet として書く。

```txt
対象:
どの機能、変更、処理か

Drivers:
要求、品質特性、制約

Priority:
どのDriverを最優先するか

Evidence / Measure:
何をもって満たしたと判断するか

Relevant axes:
今回関係する設計軸だけ選ぶ

Current design:
選んだ軸について、現在の依存、データ、transaction、外部I/O

Quality risks:
今回関係するものだけ書く
- 変更容易性
- 一貫性・データ完全性
- 性能・容量
- 可用性・回復
- 障害分離
- 観測可能性・運用性
- セキュリティ
- 開発者認知負荷

Driverは達成したい品質と優先度。
Quality risksは、現在案や代替案で満たせない可能性、検証が必要な不確実性。

Problems:
どの変更や障害が難しいか

Alternatives:
対象軸について最低2案

Decision:
どの案を選ぶか

Trade-offs:
何を得て何を失うか

Unknowns:
追加調査が必要なこと

Review Conditions:
どの条件で見直すか
```

<!--
話すこと:
- 模範解答をすぐ見せるのではなく、書かれた判断を講師またはチームメンバーがレビューする。
- コード全体ではなく、変更が多い箇所、transaction境界が曖昧な箇所、外部I/Oがある箇所などを短く切り出す。
- この作業を30分で初見から行うのは難しい。事前課題か60〜90分枠を推奨する。
-->
---

## Worksheet から ADR へ落とす

Architecture Review Worksheet は、複数の論点を広く見るためのもの。
ADR は、その中の重要な一つの判断を記録するもの。

混ぜない例:

- 請求書発行コードをユースケース単位に配置する
- メールAPIを Output Port の外へ隔離する
- 請求書更新とメール送信要求を同一DB transactionで永続化する
- 現時点では CQRS を採用しない

変更理由と見直し条件が違うなら、ADR も分ける。

<!--
話すこと:
- 1つのADRに複数の決定を詰めると、将来一部だけ見直したいときに更新しづらい。
- 最終回では、Worksheetで広く見てから、一番重要な一判断だけをADRにする。
-->
---

## レビューで見る領域

| 領域 | 確認すること |
|---|---|
| コード配置 | 変更単位と配置が合っているか |
| 依存境界 | 外部技術や失敗契約をどこで変換しているか |
| 業務ロジック | Transaction Script / Domain Model のどちらが複雑さに合うか |
| 永続化 | Write側永続化、Read側取得、Read側モデル、Mapper を混同していないか |
| 一貫性 | transaction、unique constraint、冪等性が必要か |
| メッセージング | Message semantics、応答方式、連携方式、配信形態、Delivery semantics を混同していないか |
| モジュール・所有境界 | 不変条件、データ書き込み責任、デプロイ、運用責任の境界が明確か |
| 読み書き | CQRS が必要なほどモデルやスケールが違うか |
| 性能・容量 | 主要シナリオのレイテンシ、スループット、データ量を満たせるか |
| 可用性・回復 | どの障害まで継続し、どう再試行・復旧するか |
| 観測可能性・運用性 | 失敗を検出し、原因を追跡し、再処理できるか |
| セキュリティ | 自力で解決しきれない論点を専門レビューへ渡せるか |

これは「Vertical Slice + Hexagonal だから正しい」ではない。
各軸で必要なトレードオフだけ選ぶ。

<!--
話すこと:
- 支払い/メールPortの理由を「差し替え」だけにしない。失敗契約の変換も境界を作る理由になる。
- 一覧までRepositoryに通さない判断も説明する。
-->
---

## 参考: ADR 例

```txt
Status:
  Accepted

Date:
  2026-XX-XX

Owners:
  Invoice team

Decision:
  請求書更新とメール送信要求を同一DB transactionで永続化する。

Context:
  メールAPIは一社固定だがタイムアウトが多い。
  監査ログは請求書更新と一緒に必ず残す必要がある。

Reason / Rationale:
  DB更新と「メールを送るべき」という意図を同じtransactionで残したい。
  メール送信自体は遅れてもよく、失敗時に再試行したい。

Consequences / Trade-offs:
  後続処理のworker、再送監視、冪等な処理が必要になる。

Decision Rules / Constraints:
  請求書保存とメール送信要求の保存は同じDB transactionに含める。
  外部メールAPI呼び出しはDB transaction内で行わない。
  Job Queue / Outbox Relay の選択と、Command/Eventの選択はこのADRでは固定しない。

Review Conditions:
  メール送信が同期応答に必須になったら、状態管理と補償を再検討する。
  請求書更新とメール送信要求を同じDBへ保存できなくなったら再検討する。
  通知要求テーブルの量がDB運用へ影響し始めたら再検討する。

Rejected Options:
  同期メール送信: メールAPI障害が請求書発行の可用性へ直結する。
  DB commit後の直接publish: DB更新成功後に通知要求を失う可能性がある。

Related Decisions:
  ADR: 請求書発行コードをユースケース単位に配置する。

Follow-up Decisions:
  ADR-013 または Issue #123:
    後続処理をTransactional Job Queueにするか、Outbox Relay + Brokerにするか。
  ADR-014 または Issue #124:
    メール通知をCommand Messageにするか、InvoiceIssued Eventにするか。
```

<!--
話すこと:
- Review 条件まで書くことが重要。将来の自分たちが見直せる。
- Follow-up Decision は別ADRまたはIssueへリンクする。Owner、期限、Statusを持たせると放置されにくい。
-->
---

## 到達度の評価

各項目を 0〜2 点、または N/A で見る。

| 点 | 状態 |
|---|---|
| 0 | 観点が出てこない |
| 1 | 講師の問いかけがあれば出せる |
| 2 | 自律的に具体化し、判断へ利用できる |
| N/A | 今回の問題に直接関係しない理由を説明できる |

N/Aは点数計算から除外する。
常に評価する項目には原則使用しない。
問題に関係する場合に評価する項目のみ、関係しない理由を説明できた場合に使用する。

常に評価する項目:

- 問題設定
- Driver
- スコープ
- 設計軸
- 代替案
- Trade-off
- 判断
- 不確実性
- 見直し条件

問題に関係する場合に評価する項目:

- Failure mode
- 品質特性

| 項目 | 2点の状態 |
|---|---|
| 問題設定 | 解決したい問題を具体化できる |
| Driver | 要求、品質特性、制約を具体的なシナリオ、優先順位、判断材料として表現できる |
| スコープ | コード、モジュール、プロセス、デプロイ、データ所有、チーム所有のうち、問題に関係するスコープを区別できる |
| 設計軸 | 異なる軸のパターンを混同しない |
| 代替案 | 現行案を含む、制約下で成立し得る代替案を最低2案比較できる |
| Trade-off | 利点とコストを両方説明できる |
| Failure mode | 並行実行、timeout、部分失敗、重複、順序、再試行・回復のうち、問題に関係するFailure modeを特定できる |
| 品質特性 | 性能、可用性、運用性、セキュリティなど、追加計測や専門レビューが必要な論点を識別できる |
| 判断 | 制約に基づいて一案を選べる |
| 不確実性 | 足りない情報を言える |
| 見直し条件 | 将来の再判断条件を残せる |

合計点は参考値。
常に評価する項目に0点がないか、問題に関係する項目をN/Aにした理由が妥当か、前後比較でどの観点が伸びたかを重視する。
前後比較では、常に評価する項目を中心に見て、問題依存項目は定性的に比較する。
ただし、Failure mode、整合性、品質特性に関わる重大な見落としがある場合は、点数だけで到達度を判断しない。
性能、可用性、運用性、セキュリティは、この勉強会だけで解決能力までは評価せず、追加計測や専門レビューが必要な論点として識別できるかを見る。

<!--
話すこと:
- 評価は暗記確認ではなく、初見の実コードに対して判断を言語化できるかを見る。
- 点数は序列化ではなく、次にどの能力を伸ばすかを見つけるために使う。
- 重要な観点の抜けをレビューで拾う。点数だけで合否を決めない。
-->
---

## 採用しない判断も残す

| 選択肢 | 今回採用しない理由 |
|---|---|
| 全SliceでPorts and Adapters | 単純CRUDには過剰 |
| 全テーブルRepository | Queryや集計まで意図が曖昧になる |
| CQRS | 読み取り要件がまだ単純 |
| Event Sourcing | イベント列を状態の正本にする要求がない |
| Microservices | 独立デプロイやデータ所有権分離の要求がまだ弱い |

採用しなかった理由が残ると、流行ではなく制約から説明できる。

<!--
話すこと:
- 「やらない」ことを残すと、後でなぜ採用していないのかを説明できる。
-->
---

## 中心メッセージ

<div class="study-note">
パターンは万能な正解ではない。特定の文脈で繰り返し現れる問題に対する、名前の付いた解決構造であり、利点とコストを伴う。
</div>

<!--
話すこと:
- 最後は既存構成の正当化ではなく、設計を見直し続ける姿勢で締める。
- 現在の構成も、要求と制約が変われば見直す。
-->
